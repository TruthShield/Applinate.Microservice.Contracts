namespace Applinate
{
    /// <summary>
    /// Class Builder. This class cannot be inherited.
    /// 
    /// This is a fluent builder class to be used for finding orchestration, integration, and logic queries and commands.
    /// </summary>
    /// <example>
    /// <see cref="Build"/>.<see cref="Builder">A().ContentManager().AdministrationCommand(...);
    /// </example>
    public sealed class Builder{}
}
